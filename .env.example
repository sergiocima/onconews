# OncoNews - Variabili d'Ambiente
# Copia questo file in .env e inserisci i tuoi valori

# News API Key (obbligatoria)
# Ottieni la tua chiave gratuita su: https://newsapi.org/register
NEWS_API_KEY=your_newsapi_key_here

# Database URL (PostgreSQL per Render, SQLite per locale)
# Su Render questa variabile viene configurata automaticamente
# Per test locale con PostgreSQL usa:
# DATABASE_URL=postgresql://user:password@localhost:5432/onconews
# Per SQLite (locale) non impostare questa variabile
# DATABASE_URL=

# Porta Web Server (opzionale, default: 5000)
# Su Render viene impostata automaticamente
# PORT=5000
